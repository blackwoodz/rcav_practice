<div class="row">
  <div class="col-md-12">
    <blockquote>
      <div class="page-header">
        <h1>Payment</h1>
      </div>

      <p class="lead">A <%= @years %> year loan of $<%= @principal %>, with an annual interest rate of <%= @apr %>%, requires a monthly payment of $<%= @monthly_payment.round(2) %>.</p>
    </blockquote>

    <hr>
  </div>
</div>
